[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
markers =
    unit: fast unit tests with isolated dependencies
    integration: tests that exercise multiple components or persistence
